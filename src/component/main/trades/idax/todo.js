export const useBuyPriceIdr = x => {
  return x.priceidr || [];
};

export const useBuyPriceBtc = x => {
  return x.pricebtc || [];
};

export const useSellPriceIdr = x => {
  return x.priceidr || [];
};

export const useSellPriceBtc = x => {
  return x.pricebtc || [];
};
